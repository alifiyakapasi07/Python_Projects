# Any Password will generate
# you just need to enter lenght of password you want

import random
passlen = int(input("enter the length of password :- "))
s="abcdefghijklmnopqrstuvwxyz01234567890ABCDEFGHIJKLMNOPQRSTUVWXYZ!@#$%^&*()?"    # all possible value of password are included
p = "".join(random.sample(s,passlen ))                                            # generate random from s(string) and join according to password length
print(p)
