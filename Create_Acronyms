#Create Acronyms from Phrase
#By spliting whole phrase

user_input = str(input("Enter a Phrase: "))
text = user_input.split()     #split from where space is started
a = " "
for i in text:
    a = a+str(i[0]).upper()   #join all fisrt letter of word and capitalize it
print(a)
